{
  "name": "claude-terminal",
  "version": "0.1.0",
  "description": "A browser tab bar for Claude Code web terminals (ttyd), plus self-hosted, SQLite-backed usage tracking with a live dashboard.",
  "type": "module",
  "scripts": {
    "server": "bun run server.ts",
    "collect": "bun run collector.ts",
    "migrate": "bun run migrate-state.ts",
    "build:app": "bun build app/main.tsx --outdir public/app/assets --minify && cp app/index.html public/app/index.html && cp app/styles.css public/app/assets/styles.css"
  },
  "license": "MIT",
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.246",
    "marked": "^18.0.11",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.5"
  }
}
